import 'package:flutter/material.dart';

class ConnectedAPI extends StatelessWidget {
  const ConnectedAPI({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text('This is Connected API'),
    );
  }
}